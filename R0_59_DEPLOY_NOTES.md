# R0.59 — Recruitment redesigned (openings list) + button standard in use

## Recruitment (self-styled page — list view converted)
- Hero header (dark->orange) with open-position count.
- "+ New opening" is now the orange pill.
- Intro as a warm banner; opening rows are warm rounded cards with hover + Fraunces titles.
- Stage chips restyled to the People chip look.
- Honest scope: the LIST view is done. The candidate board (kanban) + candidate detail
  modal inside an opening still use their older styling — they work fine; I'll match them
  in a later pass so this ship stays clean.

## CHECK THIS: open #hr/recruitment (Recruitment in the sidebar) — the openings list.

## Carried (cumulative)
HR Queue (pills), Home, Profile, app-wide theme, table polish, salary self-view,
attendance calendar, 4-tier roles (+ migration 32), People hub. Old Employment nav kept.

## Validation
- node --check clean: recruitment.js, hr-queue.js.
